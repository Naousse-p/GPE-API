// mocha.config.js
require("module-alias/register");

module.exports = {
  // Options de configuration Mocha
  // Vous pouvez définir ici les options de Mocha que vous souhaitez utiliser
  // Par exemple :
  reporter: "spec", // Le rapporteur de tests à utiliser
  timeout: 20000, // Le délai d'attente maximum pour chaque test
};
